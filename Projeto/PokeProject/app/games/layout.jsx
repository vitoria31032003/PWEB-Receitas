import "../globals.css";

export const metadata = {
  title: "MorphGames",
  description: "Games Page - MorphGames",
};

export default function GamesLayout({ children }) {
  return (
        <>{children}</>
  );
}
