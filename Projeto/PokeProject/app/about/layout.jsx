import "../globals.css";

export const metadata = {
  title: "MorphGames",
  description: "About Page - MorphGames",
};

export default function AboutLayout({ children }) {
  return (
        <>{children}</>
  );
}
